const initialState = {
    test: "TEST"
};

const productReducer = (state = initialState, action) => {
    return state;
    // switch (action.type) {
    //     case :

    //         break;

    //     default:
    //         break;
    // }
}

export default productReducer;